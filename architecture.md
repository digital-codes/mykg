# Architecture

Last reviewed: 2026-05-25 (Third four-agent review — merge pipeline, walkthrough health status, merge re-entry correctness)

## System Overview

mykg is a two-pass knowledge graph extractor that reads a directory of Markdown files and produces three parallel output families: `nodes.jsonl` + `edges.jsonl` for property graph consumers, `knowledge_graph.ttl` (pure RDFS Turtle) for OWL toolchains, and seven NetworkX formats (GML, GraphML, GEXF, Pajek, JSON node-link, edge list, adjacency list) in `output/networkx_output/` for graph analysis and web visualization tools. Pass 1 induces a global RDFS schema from batched file chunks; Pass 2 extracts typed node/edge instances per file against that schema; the assembler deduplicates, confidence-scores, and materialises the sidecar and final outputs. All pipeline state is persisted to intermediate JSON files so any stage can be re-entered without re-running upstream work. A two-stage orphan-connection pass runs after assembly to reconnect zero-edge nodes via co-occurrence heuristic + LLM confirmation. A two-tier correction model (KG-level correction + schema-level escalation via `SchemaUpdatedError`) handles failures automatically before falling back to human re-entry.

## Architecture Diagram

```
CLI (cli.py)
  └── Orchestrator (orchestrator.py)
        └── while True loop (schema restart guard)
              └── Pipeline steps (pipeline.py → steps/)
                    │
                    ├── ingest          → intermediate/file_manifest.json
                    │                     intermediate/base_schema_parsed.json (if --base-schema)
                    │                     intermediate/thesaurus_parsed.json (if --thesaurus)
                    ├── pass1           → intermediate/schema.json
                    │                     intermediate/schema.ttl
                    │                     intermediate/merge_log.json (synonym events)
                    ├── schema_validate → intermediate/schema_validation_errors.json (failure only)
                    │                     intermediate/schema_validate.done (sentinel)
                    ├── human_review    → intermediate/schema_approved.flag (--review mode)
                    ├── schema_flatten  → intermediate/flattened_schema.json
                    ├── pass2           → intermediate/raw_extractions.json
                    │                     intermediate/chunk_node_index.json
                    ├── normalize_names → intermediate/name_normalization.json
                    ├── assemble        → intermediate/edge_metadata.json
                    │                     intermediate/nodes.json
                    │                     intermediate/merge_log.json (dedup events)
                    ├── orphan_score    → intermediate/orphan_candidates.json
                    ├── orphan_connect  → intermediate/orphan_connections.json
                    │                     intermediate/orphan_log.json
                    │                     intermediate/schema_gap_proposals.json (if schema-gap)
                    │                     (merges into edge_metadata.json in-place)
                    │                     ↑ may raise SchemaUpdatedError → restart while loop
                    └── export          → output/nodes.jsonl
                                          output/edges.jsonl
                                          output/knowledge_graph.ttl
                                          output/knowledge_graph_validation.json
                                          output/networkx_output/  (when networkx_enabled: true)

LLM adapters (llm/)
  ├── AnthropicAdapter    (provider: anthropic)
  ├── OllamaAdapter       (provider: ollama)
  ├── OpenAIAdapter       (provider: openai)
  ├── OpenRouterAdapter   (provider: openrouter)
  └── ClaudeCLIAdapter    (provider: claude-cli — uses `claude -p` subprocess; serial only, max_workers=1)
  All selected at startup by load_adapter() from pipeline_config.yaml → provider

Config (config.py)
  └── Loads pipeline_config.yaml at import time; exposes named constants.
      No hardcoded literals anywhere in pipeline or adapter code.
```

## Design Decisions

Key decisions from CLAUDE.md that shape the structure (full record in CLAUDE.md):

- **D3** — Provider-agnostic LLM via `LLMAdapter` ABC; one method (`complete`); swapped at startup.
- **D4** — Sequential two-pass with global schema; Pass 2 can be parallelised per-file.
- **D5/D7** — Schema is `concepts[]` + `properties[]`; relationship types are `rdf:Property`, never classes.
- **D6** — Store own attributes only; flatten per-concept inheritance chain before Pass 2 (`schema_flatten` step).
- **D8/D15** — Edge metadata lives in `edge_metadata.json` sidecar only; TTL is pure RDFS with no metadata.
- **D9** — Every attribute carries `{value, confidence}`; missing attributes are `{null, 0.0}`, never dropped.
- **D16/D26** — All intermediate state persisted; four re-entry points (A=schema, B=extraction, C=assembly, D=orphan pass).
- **D17** — Automated schema validation with one LLM correction retry; optional human review gate.
- **D18** — Python library is primary; CLI wraps it.
- **D21** — `synonym_match` is purely lexical (PascalCase split + normalise + optional SKOS thesaurus).
- **D29** — Node aliases: flat `list[str]` of non-canonical surface forms; absent (not `[]`) when normalization disabled; emitted as `skos:altLabel` in TTL ABox.
- **D30** — Two-stage orphan-connection pass: Stage 1 co-occurrence heuristic → Stage 2 LLM confirmation with all name mentions via `_extract_relevant_excerpt()`.
- **D31** — Two-tier correction model: Tier 1 KG-level (orphan reconnection, name normalization, per-step retry) and Tier 2 schema-level escalation (`SchemaUpdatedError` → iterative restart, capped at `schema_max_restarts`).
- **D35** — Pass 1 runs three LLM stages per run after parallel batch induction: (1) parallel batch induction via `ThreadPoolExecutor`, (2) algorithmic merge (`merge_proposals()`), (3) harmonization LLM call (`harmonize_schema()`), (4) quality review LLM call (`review_schema_quality()`). All four stages write delta records to `schema_history/`.
- **D36** — `schema_history` module (`src/mykg/schema_history.py`) tracks schema evolution via numbered delta files per trigger (`pass1_merge`, `schema_harmonize`, `schema_quality`, `schema_gap`, `schema_gap_correct`, `schema_validate`).
- **D37** — Surgical re-extraction on schema-gap restart: shard directories are preserved; only orphan-source chunks (from `schema_hints.shared_chunks`) are re-extracted; all other file shards are reused unchanged.
- **Invariant 7** — `pipeline_config.yaml` is the sole source of truth; `config.py` exposes constants.
- **Invariant 8** — All data models use Pydantic `BaseModel`; no Python `dataclasses`.
- **Invariant 9 (Run isolation)** — Each run is fully isolated; inputs, intermediate state, outputs, and logs are co-located and self-contained. A run can always be resumed from its own snapshot without depending on external state.
- **Invariant 10 (Fine-grained resumability)** — Long-running extraction is resumable at the finest meaningful granularity. Work already completed is never repeated on restart; only the remaining work is resubmitted.
- **Invariant 11 (Durable bounded logs)** — Log output is bounded and durable. Logs are scoped to their run, rotate automatically when large, and are never silently discarded.
- **Invariant 12 (Mandatory parallelism)** — All data-processing stages that operate over independent items (files, chunks, candidates) must be parallelised using `ThreadPoolExecutor`. Serial loops over collections are a bug. Worker count is always configurable via `pipeline_config.yaml` — never hardcoded. Applies to ingest, Pass 1 batch dispatch, Pass 2 file extraction, orphan scoring, and any future bulk step.
- **Invariant 13 (LLM context and rate limit respect)** — `window_tokens + max_tokens` must never exceed the active model's context window. Worker counts must be set conservatively enough to avoid 429 rate-limit errors. A 429 is a misconfiguration signal — reduce `max_workers` in `pipeline_config.yaml`; it is not a transient error to silently retry.

## Merge Pipeline

`mykg merge-graphs <session-A> <session-B>` merges two independently-produced pipeline sessions into a new unified session. Both source sessions are read-only; all output lands in a fresh timestamped session folder. The merge reuses the same pipeline infrastructure (steps, orchestrator, assembler, exporter) as the extract pipeline via a `MergeContext` subclass and a dedicated `MERGE_STEPS` list.

### MERGE_STEPS (12 steps, in order)

| Step | Phase | Description |
|------|-------|-------------|
| `merge_setup` | 0 | Loads both `SessionData` objects, namespaces and copies shard files into the merged `intermediate/`, writes `source_map.json` |
| `merge_schema` | 1 | Runs `merge_proposals()` + `harmonize_schema()` + `review_schema_quality()` on both session schemas; writes `schema.json`, `schema.ttl`; records delta in `schema_history/` with trigger `"session_merge"` |
| `schema_validate` | 2 | Reused from extract pipeline; validates `schema.ttl` with rdflib (non-blocking advisory) |
| `human_review` | 3 | Reused; optional gate controlled by `merge_graphs.human_review` config flag |
| `schema_flatten` | 4 | Reused; writes `flattened_schema.json` for LLM prompts |
| `merge_reextract` | 5 | Re-extracts only files from sessions where the merged schema introduced new properties absent from that session's original schema; strategy is `"surgical"` (default), `"full"`, or `"none"`; surgical mode re-extracts targeted chunks with the merged schema — both net-new edges and any net-new nodes under the richer schema survive into deduplication (D38, Invariant 15) |
| `merge_raw` | 6 | Namespaces raw extractions from both sessions as `session_alias/filename` and merges into `raw_extractions.json` |
| `assemble` | 7 | Reused; deduplicates nodes and edges across both sessions; same type+name → same stable ID regardless of source session |
| `orphan_score` | 8 | Reused; maps orphan nodes from the merged graph to source chunks → `orphan_candidates.json` |
| `orphan_connect` | 9 | Reused; LLM confirms edges for each orphan group → merges `orphan_connections.json` into `edge_metadata.json` |
| `validate_graph` | 10 | Reused; exports `nodes.jsonl`, `edges.jsonl`, `knowledge_graph.ttl`, NetworkX formats |
| `merge_manifest` | 11 | Writes `merge_manifest.json` with full audit fields (non-blocking) |

### MergeContext

`MergeContext` extends `PipelineContext` with merge-specific fields. All standard pipeline step functions accept `PipelineContext` and work unchanged with `MergeContext` via Pydantic subclassing.

- `session_a_name`, `session_b_name` — source session names passed at CLI invocation
- `sessions_root` — parent directory of all sessions
- `session_a`, `session_b` — `SessionData` objects populated by `merge_setup`
- `source_map` — dict populated by `merge_setup`; maps every namespaced file key to its provenance
- `synonym_log` — list of synonym collapse events from `merge_schema`
- `schema_delta_a`, `schema_delta_b` — lists of new property names absent from each source session's original schema; populated by `merge_reextract`

### Intermediate files specific to merge

| File | Written by | Contents |
|------|-----------|----------|
| `intermediate/source_map.json` | `merge_setup` | Maps every namespaced file key (`session_alias/filename`) to provenance: original session name, alias, SHA256, role (`input_a`/`input_b`) |
| `intermediate/merge_manifest.json` | `merge_manifest` | Audit record: `session_a`, `session_b`, `merged_at`, `schema_synonym_log`, `reextraction_strategy`, `schema_delta_session_a`, `schema_delta_session_b` |

All other intermediate files (`schema.json`, `flattened_schema.json`, `raw_extractions.json`, `edge_metadata.json`, `nodes.json`, `merge_log.json`) use the same format as the extract pipeline and are produced by reused steps.

### Re-entry points for merge

- **Schema review gate:** run `mykg approve-schema --session <merged-session>` then re-run with `--session <merged-session>` to continue past the gate.
- **Re-run from any step:** pass `--session <merged-session>` to the CLI; the orchestrator resumes from the last incomplete step using the standard `_is_done` sentinel mechanism.

---

## Current State Assessment

The merge pipeline (`mykg merge-graphs`) is now fully implemented and structurally sound — the schema merge chain, file namespacing, shard copying, surgical re-extraction, and audit outputs (`source_map.json`, `merge_manifest.json`, `walkthrough.md` merge provenance) are all working correctly. The merge orchestrator correctly mirrors the extract orchestrator's `while True` / `SchemaUpdatedError` / invalidation-set pattern. Issues #91 and #81 from the prior review are confirmed resolved in the new code. The third four-agent review found a concentration of issues in: (a) the merge pipeline's `_MERGE_SCHEMA_RESTART_INVALIDATE` missing `schema_flatten` (causing silent stale-schema re-extraction on schema-gap restarts), (b) `merge_raw` re-entry breaking when sessions share filenames, (c) the walkthrough health status section reading the wrong JSON key for ABox errors (always reporting "Clean"), and (d) multiple shard file naming collision paths in the surgical merge path. Remaining open items from previous reviews: #119 (triple tokenization), #118 (prior_nodes context overflow), #83 (feedback import at module scope), #80 (unconnectable orphan advisory warnings), #79 (orphan pass type filter bypass), #77 (Army Air Forces typing).

---

## To-Do List

Items tagged: `[critical]` `[high]` `[medium]` `[low]` and `[done]` when complete.
New items added at top. Done items are never deleted — marked `[done]` for history.

| # | Priority | Area | Task | Added | Done |
|---|----------|------|------|-------|------|
| 165 | [critical] | System | **`_MERGE_SCHEMA_RESTART_INVALIDATE` missing `"schema_flatten"` — stale flat schema used on merge schema-gap restart** — In `merge_run.py`, `_MERGE_SCHEMA_RESTART_INVALIDATE` does not include `"schema_flatten"`. When `SchemaUpdatedError` fires during merge `orphan_connect`, `flattened_schema.json` is not deleted. `schema_flatten` is skipped as already done on restart. `merge_reextract` runs using the pre-gap-restart flattened schema — new property edge attribute fields are absent from the LLM prompt. Re-extracted edges for new properties have empty `attributes: {}` — Invariant 6 violation. Fix: add `"schema_flatten"` to `_MERGE_SCHEMA_RESTART_INVALIDATE` in `merge_run.py` (mirror the extract pipeline's `_SCHEMA_RESTART_INVALIDATE`). | 2026-05-25 | — |
| 164 | [critical] | Data | **`merge_raw` re-entry breaks when sessions share filenames — `ValueError` on key collision** — `step_merge_raw.run_merge_raw()` relies on `ctx.session_a.raw_extractions` already being namespaced (set by `merge_reextract`). On re-entry where `merge_reextract` was completed and is skipped, `merge_setup` repopulates `session_a.raw_extractions` with un-namespaced keys. `merge_raw_extractions()` then raises `ValueError` on the first key collision if both sessions share any filename. The docstring claims namespace is applied on re-entry but no namespace call exists in `run_merge_raw`. Fix: add unconditional `namespace_raw_extractions(ctx.session_a.raw_extractions, "session_a")` / `"session_b"` calls inside `run_merge_raw` before merging, guarded by an already-namespaced check to prevent double-prefixing. | 2026-05-25 | — |
| 163 | [critical] | System | **`merge_reextract` shard files written with un-namespaced `_fname` — orphan pass fails on re-entry** — `reextract_for_merge()` calls `run_pass2` / `run_pass2_batched` which writes shard files to `intermediate_dir / raw_extractions_shards/` with `_fname = "notes.md"` (un-namespaced, plain filename). The in-memory result is correctly namespaced afterward, but shards on disk have un-namespaced `_fname`. On re-entry, `merge_raw` reads ALL shards from disk and builds `chunk_node_index` keyed by those `_fname` values. Un-namespaced keys (`"notes.md"`) never match the `"session_a/notes.md"` keys used everywhere else. All re-extracted orphans appear unresolvable — zero connections. Fix: pass `session_alias` into `run_pass2`/`on_file_done` so shards are written with `_fname = f"{session_alias}/{original_fname}"`, or post-process shard files after `run_pass2` returns to rewrite `_fname` in place. | 2026-05-25 | — |
| 162 | [critical] | Adversarial | **Walkthrough health status always reports "Clean" ABox even when TTL validation found errors — `abox_errors` key mismatch** — In `walkthrough.py` line 1025, `_section_health_status` reads `validation.get("abox_errors", [])`. The actual key from the TTL validator is `validation.get("abox_checks", {}).get("errors", [])`. The `"abox_errors"` key does not exist — ABox errors are always `[]` in the health section. The Final Graph Summary (§1) correctly shows errors using `val.get("abox_checks", {}).get("errors", [])`. Fix: change line 1025 to `abox_errors = validation.get("abox_checks", {}).get("errors", [])`. | 2026-05-25 | — |
| 161 | [critical] | Adversarial | **`merge_raw` context `None` on cold re-entry after `merge_reextract` done — RuntimeError blocks re-entry** — When `merge_reextract` completes (sentinel written) and the process is killed during `merge_raw`, re-entry starts a fresh Python process. `ctx.session_a` is `None` (populated only by `merge_setup` in the prior process, not persisted). `merge_setup` is skipped (`_is_done` true). `run_merge_raw` checks `if ctx.session_a is None` and raises `RuntimeError`. Re-entry is impossible without deleting `merge_reextract.done` — which repeats all LLM re-extraction cost. Re-entry hint does not mention deleting the sentinel. Fix: `merge_raw` should reload session data from disk when `ctx.session_a` is None, using `load_session()` directly inside `run_merge_raw` as a cold-re-entry path. | 2026-05-25 | — |
| 160 | [critical] | Adversarial | **Self-merge (`session_a == session_b`) completes silently with misleading audit trail** — Nothing in the `merge_graphs` CLI checks whether both arguments name the same session. Both are read, namespaced, and merged. The final graph is structurally correct (node/edge deduplication collapses duplicates) but `source_map.json`, `merge_manifest.json`, and the walkthrough all report "session_a: X, session_b: X" with 100% deduplication — an operator interprets this as a valid cross-corpus merge. Fix: add an early guard in `cli.py` `merge_graphs()` that raises `ClickException` when `session_a_name == session_b_name`. | 2026-05-25 | — |
| 159 | [high] | System | **`_MERGE_SCHEMA_RESTART_INVALIDATE` missing `"schema_validate"` — new schema-gap properties bypass RDFS validation in merge** — Same as to-do #113 applied to the extract pipeline, not yet applied to `merge_run.py`. When `SchemaUpdatedError` fires, `schema_validate.done` persists and RDFS validation is skipped on restart. New properties proposed by `propose_schema_additions()` are never validated before `merge_reextract` runs them. Fix: add `"schema_validate"` to `_MERGE_SCHEMA_RESTART_INVALIDATE`. | 2026-05-25 | — |
| 158 | [high] | System | **`merge-graphs` CLI has no `--from-step` flag — documented re-entry hints reference nonexistent flag** — The `merge-graphs` Click command has no `--from-step` option. `_MERGE_REENTRY_HINTS` in `merge_run.py` documents re-entry instructions referencing `--from-step orphan_score` etc., but that flag does not exist. Fix: either implement `--from-step` for `merge-graphs` using the same `_delete_from_step` infrastructure with a MERGE_STEPS-aware step list, or update all `_MERGE_REENTRY_HINTS` to describe manual file deletion instead of referencing a nonexistent CLI flag. | 2026-05-25 | — |
| 157 | [high] | Data | **`_copy_shard_dir` shard filename collision — same slug for two different files causes last-writer-wins silent overwrite** — Long filenames are truncated and sha1-suffixed. Two files whose truncated names produce the same 16-char sha1 prefix would produce the same destination filename. The shorter shard filename collision (two files whose slugs are identical before truncation) also silently overwrites. The affected file's extractions are permanently missing from the merged session. Fix: use full content hash (sha256 of `_fname + shard_file.stem`) to generate the collision-resistant merged shard name, ensuring globally unique destination filenames across both sessions. | 2026-05-25 | — |
| 156 | [high] | System | **`step_orphan_connect` uses `ORPHAN_SCHEMA_MAX_RESTARTS` (extract config) in merge pipeline, not `MERGE_ORPHAN_SCHEMA_MAX_RESTARTS`** — The guard inside `step_orphan_connect.py` that decides whether to call the schema proposal LLM uses `_cfg.ORPHAN_SCHEMA_MAX_RESTARTS`. If `orphan_pass.schema_max_restarts: 0` (extract) but `merge_graphs.orphan_pass_max_restarts: 1` (merge), the guard skips the LLM call in merge. Dangerous direction: `ORPHAN_SCHEMA_MAX_RESTARTS = 2`, `MERGE_ORPHAN_SCHEMA_MAX_RESTARTS = 0` — `step_orphan_connect` calls the LLM and raises `SchemaUpdatedError`; `merge_run` blocks the restart; but `schema.json` was already updated inside the step. The schema silently gains spurious properties. Fix: expose a `schema_max_restarts` parameter on `run_orphan_connect()` and pass either `ORPHAN_SCHEMA_MAX_RESTARTS` or `MERGE_ORPHAN_SCHEMA_MAX_RESTARTS` from the respective orchestrator. | 2026-05-25 | — |
| 155 | [medium] | System | **`merge_reextract` calls session A then session B serially — Invariant 12 violation** — `step_merge_reextract.py` calls `reextract_for_merge("session_a", ...)` then `reextract_for_merge("session_b", ...)` sequentially. These are independent with no shared state. For `"full"` strategy on large corpora this doubles wall-clock time. Fix: wrap both calls in a `ThreadPoolExecutor(max_workers=2)`, collect futures, merge results. | 2026-05-25 | — |
| 154 | [medium] | Data | **`architecture.md` surgical re-extraction description contradicts D38 and Invariant 15** — Line 99 says "surgical mode produces only new edges and enriched node attributes — it does NOT create new nodes." The implementation (`merger.py`) merges the full `run_pass2()` output (including net-new nodes) — matching D38 and Invariant 15. Fix: update architecture.md line 99 to: "surgical mode re-extracts targeted chunks with the merged schema; both net-new edges and any net-new nodes discovered under the richer schema survive into deduplication (per D38 and Invariant 15)." | 2026-05-25 | — |
| 153 | [medium] | Data | **Walkthrough merge count probe uses wrong field (`type is None` vs `event == "node_merge"`) — node/edge merge counts swapped** — `walkthrough.py` lines 657–658 use `e.get("type") is None` to detect node merge events and `e.get("type") is not None` for edge merge events. All merge_log entries lack a `"type"` key — they use an `"event"` key. Result: all merge events counted as node merges, zero as edge merges. Fix: replace with `e.get("event") == "node_merge"` and `e.get("event") == "edge_merge"`, consistent with `_section_extraction_summary` at lines 458–459. | 2026-05-25 | — |
| 152 | [medium] | System | **`merge_manifest` re-entry on cold start writes empty `schema_delta_*` — silent audit trail corruption** — `step_merge_manifest` reads `ctx.synonym_log`, `ctx.schema_delta_a`, `ctx.schema_delta_b` from in-memory context. On cold re-entry (crash after `validate_graph`, before `merge_manifest`), all these fields are empty lists (Pydantic defaults). `merge_manifest.json` is written with `schema_delta_session_a: []` even when the actual delta was non-empty. Fix: before writing, check if ctx fields are empty; if so, reload from `schema_history/` (for deltas) and log a warning that provenance was partially recovered. | 2026-05-25 | — |
| 151 | [medium] | Adversarial | **`review_schema_quality_for_merge` can accept `{"concepts": [], "properties": []}` — complete schema deletion** — The LLM quality-review call for merge can return an empty schema. An empty TBox is valid RDFS — `schema_validate` passes. `merge_reextract` runs with empty flattened schema; every extraction is rejected as unknown type. All output files are empty but structurally valid. Fix: add a lower-bound guard in the quality-review accept path — reject the LLM result if `len(concepts) < 1` or `len(concepts) < 0.5 * len(original_concepts)` (more than 50% removed). Retry with the original schema if rejected. | 2026-05-25 | — |
| 150 | [medium] | Adversarial | **Invariant 7 partial violation in `config.py` — `ERROR_GATE_ENABLED`, `ERROR_GATE_THRESHOLD`, and merge `_get_opt` defaults applied when YAML section absent** — `config.py` uses `_eg.get("enabled", True)` and `_eg.get("threshold", 3)` and `_get_opt` for merge constants. A `pipeline_config.yaml` without `error_gate` or `merge_graphs` sections silently uses Python-level defaults. Invariant 7 requires the YAML to be the sole source of truth. Fix: change to `_eg["enabled"]` / `_eg["threshold"]` — raise `KeyError` if the section is absent; require all profiles to declare both sections explicitly. | 2026-05-25 | — |
| 149 | [low] | System | **`pipeline_state.json` step left in `"running"` state when schema-gap restart limit is hit** — In both `orchestrator.py` and `merge_run.py`, when the restart limit is reached, `continue` is called without ever calling `mark_failed` or `mark_done` on the step. The step remains `"running"` in `pipeline_state.json` permanently. Tools that read `pipeline_state.json` see an inconsistent state. Fix: add `state.mark_failed(step.name, "schema_restart_limit_reached", ...)` and `state.save(...)` before the `continue` call. | 2026-05-25 | — |
| 148 | [low] | Data | **D16 missing rows for three merge-specific intermediate files** — `intermediate/source_map.json`, `intermediate/merge_manifest.json`, and `intermediate/merge_reextract.done` are not in the D16 table. They are documented in D38 but D16 is the normative per-file reference. Fix: add three rows to D16 for these files. | 2026-05-25 | — |
| 147 | [low] | Data | **D36 trigger label list missing `"session_merge"`** — D36 lists six trigger labels; `"session_merge"` written by `step_merge_schema` is absent. Fix: add `session_merge — merged schema after cross-session merge_schema step` to the D36 bullet list. | 2026-05-25 | — |
| 146 | [low] | Data | **`architecture.md` MERGE_STEPS table says 10 steps but implementation has 12** — `architecture.md` header says "MERGE_STEPS (10 steps, in order)" and the current state assessment says "10-step MERGE_STEPS." CLAUDE.md D38 and `merge_pipeline.py` both show 12 steps. Fix: update architecture.md to say 12 steps and add `orphan_score` (Phase 8) and `orphan_connect` (Phase 9) rows to the table. | 2026-05-25 | — |
| 145 | [done] | System | **Cross-session merge graphs (`mykg merge-graphs`)** — merge two independently-produced pipeline sessions into a unified session; schema merge chain reuses `merge_proposals()` + `harmonize_schema()` + `review_schema_quality()`; file namespacing via `session_alias/filename`; `source_map.json` and `merge_manifest.json` audit files; surgical re-extraction strategy for new schema properties; `MergeContext` extends `PipelineContext` with merge-specific fields. | 2026-05-24 | 2026-05-24 |
| 144 | [done] | Software | **`_partial_recover` keeps hallucinated anchor nodes after dropping their invalid edges** — When the LLM invents a placeholder node ID (e.g. `ufo-incident-olsen-dummy`) solely to anchor edges, `_partial_recover` drops the invalid edges but keeps the node because it has a valid schema type. The node becomes a guaranteed orphan with no edges and no semantic content. Fix: after computing `valid_edges`, drop any node that (a) is new to this chunk (not in `prior_nodes`) and (b) is not referenced by any surviving edge. Nodes from `prior_nodes` are exempted — they were extracted in a prior chunk and are known-good; a future chunk may yet connect them. This only fires in degraded mode (retry already failed), so the cost of false-positive drops is low. | 2026-05-18 | 2026-05-18 |
| 143 | [done] | Software | **ErrorGate race condition — errors lost between `_count=0` and `_resume.clear()`** — Inside `record_error()`, `_count` is reset to 0 and `_resume.clear()` both happen inside `with self._lock`, BUT `_resume.wait()` is called *before* acquiring the lock. A second thread can pass `_resume.wait()` (event still set), then acquire the lock after thread A has zeroed the count but before `_resume.clear()` fires. Thread B increments count to 1 (below threshold) and returns. Thread A then clears the event. The error from thread B is permanently lost from the accumulator. Under high concurrency the effective threshold is higher than configured. Fix: move `_resume.wait()` call inside the lock, or restructure so the check and clear are atomic: acquire lock, increment, if threshold: reset count, snapshot and clear outside lock only after full lock release. | 2026-05-18 | 2026-05-18 |
| 142 | [done] | System | **`--from-step pass2` leaves shard directories intact — Re-entry B silently reuses stale extractions** — `_delete_from_step()` in `cli.py` deletes only files listed in `Step.outputs` (`raw_extractions.done`, `chunk_node_index.json`). The `raw_extractions_shards/` and `chunk_index_shards/` directories are not in that list. When `--from-step pass2` is run for Re-entry B, both shard directories survive. `step_pass2._run()` sees them, loads all existing shards as `existing_raw`, sets `skip = set(existing_raw.keys())`, and skips every previously-extracted file. Re-entry B produces exactly the same extraction as before — nothing is re-extracted. Fix: extend `_delete_from_step` to call `shutil.rmtree` on both shard directories when deleting from `pass2` or earlier. | 2026-05-18 | 2026-05-18 |
| 141 | [done] | Software | **`confirmed_orphan_ids` tracks only `edge["from"]` — reversed-direction orphan edges incorrectly promoted to schema-gap** — In `step_orphan_connect.py`, `confirmed_orphan_ids.add(edge["from"])`. When `_confirm_one()` flips edge direction (orphan is the object, not subject), `edge["from"] = candidate.candidate_id` — the orphan's ID is in `edge["to"]`, never added to `confirmed_orphan_ids`. The orphan is then treated as unconfirmed and promoted to a schema-gap orphan, triggering unnecessary schema proposals and potential Re-entry A restarts. Fix: add `"_orphan_id": candidate.orphan_id` to the dict returned by `_confirm_one()`; in `step_orphan_connect.py` use `confirmed_orphan_ids.add(edge.get("_orphan_id", edge["from"]))`. The `_orphan_id` key is stripped by the existing `not k.startswith("_")` filter before storage. | 2026-05-18 | 2026-05-18 |
| 140 | [done] | System | **`--from-step` without `--session` creates a new empty session — Re-entry is silently a no-op** — If a user runs `mykg extract-graph input_dir --from-step pass2` without `--session`, the CLI creates a new timestamped session with an empty `intermediate/`. `_delete_from_step` is a no-op (nothing to delete). The pipeline runs from scratch, ignoring prior session state entirely. D26 re-entry documentation does not mention that `--session` is required alongside `--from-step`. Fix: when `from_step` is provided and `session` is None and no explicit `--output-dir`/`--intermediate-dir`, either raise `ClickException("--from-step requires --session <name>")` or auto-select the most recent session. Update D26 re-entry examples in CLAUDE.md to include `--session`. | 2026-05-18 | 2026-05-18 |
| 139 | [done] | Software | **Error-rejected orphan candidates promoted to schema-gap — transient failures trigger schema restarts** — When `_confirm_one()` raises (429, timeout, network), the rejection gets `"reason": "error"`. The promotion loop in `step_orphan_connect.py` uses `{c.orphan_id for c in candidates} - confirmed_orphan_ids` — includes error-rejected candidates. These get fed to `propose_schema_additions()`, which asks the LLM to design new schema properties for nodes that already fit the existing schema. Fix: maintain a separate `error_rejected_orphan_ids` set from rejections where `reason == "error"`, exclude from the promotion loop. Log them as `"orphan_unconnectable_transient"` in `orphan_log.json`. | 2026-05-18 | 2026-05-18 |
| 138 | [done] | System | **Schema-gap restart does not delete shard directories — re-extracted files skipped on re-entry A** — `_SCHEMA_RESTART_INVALIDATE` in `orchestrator.py` deletes the files listed in each Step's `outputs`, but `raw_extractions_shards/` and `chunk_index_shards/` are not step outputs. On a schema-gap Re-entry A restart, both shard directories survive. `step_pass2._run()` loads existing shards, marks all previously-extracted files as done, and skips re-extraction. The new schema properties are never reflected in the extraction. Fix: add `shutil.rmtree` calls for both shard directories in the `_SCHEMA_RESTART_INVALIDATE` cleanup block of `orchestrator.py`. | 2026-05-18 | 2026-05-18 |
| 137 | [done] | Software | **Duplicate test functions in `test_steps.py` — second copy silently overwrites first, false coverage** — Three test functions are defined twice: `test_on_file_done_writes_shard_not_full_rewrite`, `test_shards_loaded_on_restart`, `test_backward_compat_monolithic_fallback`. Python silently overwrites the first definition; only the second copy runs. The first copy is dead, giving false coverage confidence. Fix: delete the duplicate definitions (lines ~604–699). | 2026-05-18 | 2026-05-18 |
| 136 | [done] | Data | **`docs/pipeline-workflow.md` is severely outdated — four false statements about the current codebase** — The document was generated from session `2026-05-18T07-09-04` (partial run, before #116/#117/#121/#122/#123 were implemented). Current false statements: (1) Ingest listed as serial D12 violation — now parallel. (2) Pass 1 listed as serial D12 violation — now parallel. (3) `on_file_done` described as O(N²) full rewrite — now per-file shard. (4) "3 batches failed JSON parse (no JSON retry)" — JSON retry now implemented. ErrorGate not mentioned anywhere. Fix: replace `docs/pipeline-workflow.md` with a current-state document reflecting all implemented changes. | 2026-05-18 | 2026-05-18 |
| 135 | [done] | Data | **`raw_extractions.done`, `raw_extractions_shards/`, `chunk_index_shards/` not in D16** — These are now the primary pass2 resumability mechanism (Invariant 10) but absent from CLAUDE.md D16 table. Re-entry B guidance ("edit `raw_extractions.json`") is now misleading — with shards, the merged file is overwritten by shard data on re-entry. Fix: add three rows to D16; update D26 Re-entry B guidance to point users to per-file shard files. | 2026-05-18 | 2026-05-18 |
| 134 | [done] | Software | **Remove dead `TYPE_CHECKING` block in `pass1.py`** — Lines 5, 14–15 of `pass1.py`: `from typing import TYPE_CHECKING` and `if TYPE_CHECKING: pass`. The block is empty and the import is unused. Cleanup only. | 2026-05-18 | 2026-05-18 |
| 133 | [done] | Software | **`hasattr(exc, "pos")` guard in `pass1.py` debug log is wrong** — `json.JSONDecodeError` always has `.pos`; the `hasattr` check is dead. The `except` clause catches both `json.JSONDecodeError` and `ValueError`; for a plain `ValueError`, `exc.pos` raises `AttributeError` rather than falling back to `raw[:200]`. Fix: change guard to `isinstance(exc, json.JSONDecodeError)`. | 2026-05-18 | 2026-05-18 |
| 132 | [done] | Software | **`config.py` loads YAML twice and assigns `RAW` twice** — Line 40: `RAW: dict = _load()` is immediately overwritten by line 73: `RAW: dict = _apply_profile(_load())`. The YAML file is parsed twice at import; the first `RAW` is never used. Fix: remove line 40; keep only line 73. | 2026-05-18 | 2026-05-18 |
| 131 | [done] | Software | **No tests for `ErrorGate`** — Threshold-trip logic, resume/pause cycle, EOFError auto-resume path, 'q' abort, and double-trip prevention are all untested. Given threading and interactive stdin, this is the least-tested new component. Fix: create `tests/test_error_gate.py` covering all code paths including concurrent record_error calls near the threshold. | 2026-05-18 | 2026-05-18 |
| 130 | [done] | System | **`openrouter-free` profile has no context window comment — Invariant 13 misconfiguration risk** — The profile sets `batch_token_target: 64000`, `max_tokens: 8192` (total 72K). OpenRouter free-tier models have varying context windows (some as small as 8K). No guard exists against a user selecting a model that cannot accommodate the configured batch size. Fix: add a comment listing minimum required context window; add guidance to set `llm.model` to a specific model rather than `openrouter/free`. | 2026-05-18 | 2026-05-18 |
| 129 | [done] | Data | **`orphan_connect` crash between `orphan_connections.json` write and `edge_metadata.json` merge — edges permanently lost** — `step_orphan_connect` writes `orphan_connections.json` first, then merges into `edge_metadata.json`. If the process crashes in this window, `orphan_connections.json` exists (step looks done to `_is_done`), but the edges are not in `edge_metadata.json`. Re-entry skips `orphan_connect` because the output file exists — the edges are permanently missing. Fix: write `edge_metadata.json` first (or atomically), then write `orphan_connections.json` as the completion sentinel. | 2026-05-18 | 2026-05-18 |
| 128 | [done] | Software | **`pass2.py` `fname` variable shadowing in `as_completed` loop** — `fname = futures[future]` (line 453) is immediately shadowed by `fname, result, file_idx = future.result()` (line 455). On the error path, the outer `fname` is used for logging — correct but fragile. If `_process_file` ever returns a different filename, `results` would use the inner name while the skip-list uses the outer name, causing double-extraction or missed extraction. Fix: rename the outer lookup to `candidate_fname` to make the distinction explicit. | 2026-05-18 | 2026-05-18 |
| 127 | [done] | Data | **`_dedup_within_file` drops duplicate-slug nodes — edges referencing dropped IDs become dangling** — Two nodes in one file with different `id` fields but the same name-type slug: the second is dropped. An edge referencing the dropped ID passes `validate_extraction` (IDs were present at validation time), then fails resolution in `assign_stable_ids`. The dangling reference propagates into all output formats; the ABox validator catches it but only as an advisory warning. Fix: after `_dedup_within_file`, revalidate edges against the surviving node IDs and log/drop dangling references. | 2026-05-18 | 2026-05-18 |
| 126 | [done] | Data | **`_copy_input_files` preserves subdirectory structure but D32 specifies flat copy** — `cli.py` uses `input_dir.rglob("*.md")` with `target.parent.mkdir(parents=True)`, preserving subdirectory hierarchy. D32 says "flat-copies all files." The behavior is arguably better, but creates a documentation inconsistency. Fix: update D32 to document recursive copy behavior, or change implementation to `input_dir.glob("*.md")` for true flat copy. | 2026-05-18 | 2026-05-18 |
| 125 | [done] | Software | **Invariant 5 bypass: `Relationship` class can enter `schema.json` from Pass 1** — The Pass 1 system prompt instructs the LLM not to add a `Relationship` class, but `merge_proposals` in `schema_merge.py` does not filter it. A `Relationship` concept proposed by the LLM propagates into `schema.json` and then into all outputs. `validate_extraction` in pass2 would accept nodes of that type (it's declared). Fix: add a filter in `merge_proposals` rejecting any concept whose `type.lower() == "relationship"`. | 2026-05-18 | 2026-05-18 |
| 124 | [done] | Software | **Hardcoded `timeout=120` fallback in `llm/config.py` — Invariant 7 violation** — `section.get("timeout", 120)` for anthropic, openrouter, and openai providers silently uses 120s if `llm.timeout` is absent from a profile. Invariant 7 requires the YAML to be the single source of truth. Fix: change to `section["timeout"]` (consistent with Ollama behavior) — raise `KeyError` if missing rather than silently applying a default. Note: `openrouter-free` profile currently lacks `llm.timeout` and must be updated first. | 2026-05-18 | 2026-05-18 |
| 123 | [done] | System | **HTTP 429 from Ollama crashes Pass 2 with no backoff — all progress lost** — `OllamaAdapter.complete()` raises `httpx.HTTPStatusError` on a 429 response. The exception propagates through `llm_complete_with_retry` (which only retries empty responses, not exceptions), through `_extract_chunk`, through `_process_file`, and kills the entire `ThreadPoolExecutor` future. The orchestrator catches it as a generic step failure and retries the whole step from scratch — all completed file extractions are discarded. Session `2026-05-18T07-09-04` hit this 3 times, losing all Pass 2 progress each time. Fix: add 429-specific retry with exponential backoff inside each adapter (`httpx.HTTPStatusError` with status 429 → sleep `2^attempt` seconds, retry up to 5 times before re-raising). Same fix needed for `AnthropicAdapter` and `OpenAIAdapter`. Unlike empty-response retry (LLM indeterminacy), 429 retry is correct: Ollama will accept the request once the queue drains. Config: add `llm.retry_429_max: 5` and `llm.retry_429_base_delay: 2.0` to each profile in `pipeline_config.yaml`; expose as `LLM_RETRY_429_MAX` and `LLM_RETRY_429_BASE_DELAY` in `config.py`. | 2026-05-18 | 2026-05-18 |
| 122 | [done] | System | **Pass 1 JSON parse failures are silent — batches skipped with no retry** — when `json.JSONDecodeError` is raised on the LLM response in the Pass 1 batch loop (`pass1.py`), the batch is logged as a warning and skipped. Session `2026-05-18T07-09-04`: 3 of 40 batches lost this way (2 hit `max_tokens` ceiling → truncated JSON; 1 returned non-JSON). `llm_complete_with_retry` only guards against empty/whitespace responses — it does not retry on parse failure. `_extract_chunk` in pass2 has a JSON retry (appends "not valid JSON" prefix); pass1 has no equivalent. Fix: wrap the `json.loads()` call in `pass1.py` with a 1-retry loop: on `JSONDecodeError`, call the LLM again with a "Your previous response was not valid JSON. Return only a JSON object with `concepts` and `properties` keys." prefix. Context label: `f"pass1 batch {i}/{len(batches)} json-retry"`. No new config key needed — reuse `llm_complete_with_retry` inside the retry loop. | 2026-05-18 | 2026-05-18 |
| 121 | [done] | System | **`raw_extractions.json` and `chunk_node_index.json` fully rewritten on every file completion — O(N²) disk writes** — `on_file_done` callback in `pass2.py` serialises both files to disk after each of the 288 file completions: `json.dump(all_data)` every time. At 288 files × average shard size, total bytes written ≈ N×(N+1)/2 × avg_shard ≈ 41,000+ write operations of growing size. For 10K-file corpora this is O(N²). Fix: shard approach — each file writes its own `raw_extractions/<fname_slug>.json` shard during `on_file_done`; the assembly step reads all shards and merges. `raw_extractions.json` is still written as the merged result after all files complete, preserving backward compatibility. Same sharding for `chunk_node_index.json`. Per-file shard enables: (a) per-file re-run resumability — skip already-extracted files by checking shard existence; (b) eliminates the O(N²) rewrite bottleneck. `on_file_done` becomes a single-file write only. | 2026-05-18 | 2026-05-18 |
| 120 | [done] | Data | **`chunk_node_index.json` stable IDs drift after `normalize_names` — orphan Stage 1 uses wrong IDs** — `chunk_node_index.json` is built by `pass2._process_file` using stable IDs computed from the raw LLM-extracted node name before `normalize_names` runs. After name normalization, canonical names change, which changes `name_slug`, which changes stable IDs in `nodes.json` and `edge_metadata.json`. But `chunk_node_index.json` still holds the pre-normalization IDs. Stage 1 orphan scoring uses `chunk_node_index.json` to look up nodes present in each chunk; the IDs it finds no longer match `nodes.json`. A normalized alias that becomes a new canonical node gets a different stable ID and may be scored as orphaned when it isn't. Fix: rebuild `chunk_node_index.json` at the end of `step_normalize_names` using the final canonical IDs from `name_normalization.json`. Add `"chunk_node_index.json"` to `normalize_names` step outputs so `_is_done` detects it as stale if normalization output changes. | 2026-05-18 | 2026-05-18 |
| 119 | [medium] | System | **Files re-tokenized three times — triple tiktoken cost** — tiktoken encoding runs once in `step_ingest` (to chunk all files → `ctx.all_chunks`), once per file in `pass2._process_file` (re-chunking from raw content to get local chunk objects), and once in `orphan_connect.build_chunk_texts` (re-chunking from `file_manifest.json` to extract source excerpts for Stage 2 prompts). The pass2 re-tokenization is necessary when `ctx.all_chunks` is not passed through (it is already chunked — the issue is that workers receive raw content and re-chunk). The orphan re-tokenization is avoidable if chunk texts were written to an intermediate file. Fix (incremental): (a) in `pass2._process_file`, check if the chunk objects are already in `ctx.all_chunks` for this file before re-chunking — if so, filter and reuse; (b) in `step_orphan_connect`, if per-file chunk shard files exist (from #121 fix), read chunk texts directly from shards instead of re-tokenizing. Full fix blocked by #121 (shard approach). | 2026-05-18 | |
| 118 | [medium] | System | **Pass 2 prompt grows unboundedly as `prior_nodes` block accumulates — context window overflow for large files** — in `_extract_chunk`, the prior-nodes block includes all node IDs extracted so far within the file (`prior_nodes`). For files with many chunks, by chunk 3+ this block can reach 27K tokens (observed in session `2026-05-18T07-09-04`). Schema block is 7,984 tokens; chunk text is up to 32K. Total = 67K tokens input, approaching Ollama gemma4:31b-cloud 256K limit safely for now, but for GPT-4o (128K context, 16K output, 96K max input), chunk 3 of a large file would overflow. Fix: cap the prior-nodes block at a configurable token budget (suggested `pipeline.pass2.prior_nodes_max_tokens: 8000`) — if the full prior-nodes serialisation exceeds the cap, truncate to the most-recently-extracted nodes (most relevant for adjacent chunks). Expose `PASS2_PRIOR_NODES_MAX_TOKENS` in `config.py`. This is a correctness fix for GPT-4o and future smaller-context models; Ollama gemma4 is safe today. | 2026-05-18 | |
| 117 | [done] | System | **Pass 1 LLM batch calls are serial — Invariant 12 violation** — `pass1.py` fires one LLM call per batch in a plain `for i, batch in enumerate(batches, 1)` loop (line 71). Each batch is an independent LLM call with no dependency on other batch results; they can all run concurrently. Fix: replace the loop with a `ThreadPoolExecutor` whose worker count is configurable via `pipeline_config.yaml → pipeline.pass1.max_workers` (default 4 — LLM calls are network-bound, not CPU-bound, so fewer workers than ingest). Each worker calls `adapter.complete(system, user_text)` for one batch and returns `(batch_index, proposal_or_None)`. Main thread collects futures, filters None (parse errors), and passes the list to `merge_proposals`. Batch index must be preserved for log ordering. Same error policy as pass2: JSON parse errors are warnings (skip batch), unexpected exceptions re-raise. New config key: `pipeline.pass1.max_workers`. | 2026-05-18 | 2026-05-18 |
| 116 | [done] | System | **`run_ingest` reads files serially — Invariant 12 violation** — `step_ingest.py` uses a plain `for md_file in md_files` loop to read, hash, and chunk each file. With 288+ files this is single-threaded. **Design (four-agent review 2026-05-18):** (1) Extract `_ingest_file(md_file, input_dir) → (rel, content, sha256, chunks)` — pure function, no shared-state writes inside worker. (2) Main thread submits all files to `ThreadPoolExecutor(max_workers=INGEST_MAX_WORKERS)`, collects futures via `as_completed`. **Critical:** after all futures resolve, merge results in `sorted(results.keys())` order — NOT completion order — before building `ctx.all_chunks`. Completion-order merge makes chunk ordering non-deterministic, which changes Pass 1 batch boundaries, which makes `schema.json` non-deterministic across runs and breaks Re-entry B reproducibility. (3) After sorted merge, do an explicit `ctx.all_chunks.sort(key=lambda c: (c.source_file, c.chunk_index))` as a belt-and-suspenders guard. (4) `_run_append_ingest` gets a separate lighter `_read_and_hash(md_file, input_dir) → (rel, content, sha256)` worker — NO `chunk_file` call (append does not need chunks; accidentally populating `ctx.all_chunks` in append mode would cause partial schema induction from changed files only). Keep the second `unextracted` loop (lines 96–112) serial in the main thread — parallelising it risks a `RuntimeError: dictionary changed size during iteration` Heisenbug. (5) Error policy: catch `OSError`/`UnicodeDecodeError` per future using a `future → fname` map, log warning with filename, skip file — do not abort full ingest for one unreadable file. (6) Config: add `pipeline.ingest.max_workers: 8` to `pipeline_config.yaml` (after `chunking:` block); add `INGEST_MAX_WORKERS: int = _get("ingest", "max_workers")` to `config.py`; add `ingest_workers` field to `PipelineContext` mirroring `pass2_workers`. Update YAML and config.py in the same commit to avoid import-time `KeyError`. (7) `file_manifest.json` written once at end — no incremental flush needed (ingest is fast and fully idempotent). | 2026-05-18 | 2026-05-18 |
| 115 | [done] | System | **`--from-step pass1` crashes with `TypeError` on new `file_manifest.json` format** — `step_pass1.py` recovery path reads `file_manifest.json` and iterates `for fname, content in file_contents.items()`, passing `content` directly to `chunk_file(fname, content)`. Since the manifest migration, values are `{"content": "...", "sha256": "..."}` dicts, not strings. `_strip_frontmatter(content)` calls `re.sub()` on a dict → `TypeError`. `step_pass2` handles this correctly via `_content_from_entry()` but `step_pass1` does not. Fix: add `_content_from_entry()` extraction in `step_pass1.py`'s recovery path. This blocks Re-entry A (D26). | 2026-05-17 | 2026-05-17 |
| 114 | [done] | Data | **Orphan confirmation confidence not clamped to `[0.0, 1.0]`** — `orphan_connector._confirm_one` reads `llm_conf = float(result.get("confidence", ...))` with no clamping. LLM can return `2.5` → `final_conf = 2.25` or `-0.3` → `final_conf = -0.27`. These propagate to `edge_metadata.json`, `edges.jsonl`, and all NetworkX exports silently. `_coerce_attr` clamping that protects extracted edges is not applied to orphan-inferred edges. Fix: clamp `llm_conf = max(0.0, min(1.0, float(...)))` before the confidence formula, and clamp `final_conf` as well. | 2026-05-17 | 2026-05-17 |
| 113 | [done] | System | **`schema_validate` step not invalidated on automated schema-gap restart — LLM-modified schema bypasses RDFS validation** — `_SCHEMA_RESTART_INVALIDATE` does not include `"schema_validate"`. When `SchemaUpdatedError` fires, `schema_validate.done` persists, `_is_done("schema_validate")` returns True, and the step is skipped on restart. New properties proposed by `propose_schema_additions` are never validated through D17's LLM-correction retry cycle before Pass 2 re-runs. Fix: add `"schema_validate"` to `_SCHEMA_RESTART_INVALIDATE` in `orchestrator.py`. | 2026-05-17 | 2026-05-17 |
| 112 | [done] | System | **Human review gate bypassed by automated schema-gap restart when `--review` is active** — `schema_approved.flag` is not deleted during `_SCHEMA_RESTART_INVALIDATE` cleanup (neither `"human_review"` nor its output is in the set). After a schema-gap restart adds new properties, the gate is silently skipped and the user never reviews the LLM-modified schema. Same issue for `--from-step pass2 --review`: `_delete_from_step` deletes from `pass2` onward but `human_review` (index 3) comes before `pass2`, so the flag persists. Fix: add `"human_review"` to `_SCHEMA_RESTART_INVALIDATE`; in `_delete_from_step`, always delete `schema_approved.flag` when the target step is at or after `schema_flatten`. | 2026-05-17 | 2026-05-17 |
| 111 | [done] | Software | **`_partial_recover` accepts nodes of undeclared types — ghost nodes bypass Invariant 5 and Invariant 6** — When the LLM includes a `Relationship` node in the retry extraction, `validate_extraction` catches it as an unknown type error but `_partial_recover` accepts ALL nodes without any type check (only edges are filtered). The ghost node passes through `_backfill_extraction` with `expected = flat_schema.get("Relationship", [])` = `[]` — no null-filling, violating Invariant 6. It then propagates to `nodes.jsonl`, `knowledge_graph.ttl` (undeclared `rdf:type`), and all NetworkX exports. Fix: add a type-whitelist check in `_partial_recover` — reject nodes whose `type` is not in `flat_schema`. | 2026-05-17 | 2026-05-17 |
| 110 | [done] | Software | **`validate_extraction` does not check required node fields — `KeyError` crashes downstream** — No validation that each node has `"id"` (non-empty string), `"confidence"` (float), or that `"attributes"` is a dict. If LLM returns `{"type": "Person"}` (missing `id`), validation passes but `_dedup_within_file` and `_name_slug` crash on `node["id"]`. Also `validate_extraction` accepts the `flat_schema` parameter but never uses it — attribute completeness is never checked at validation time. Fix: add explicit field presence checks for `id`, `type`, `confidence`, and `attributes` type in the node validation loop. | 2026-05-17 | 2026-05-17 |
| 109 | [done] | Software | **`network.py` duplicates NetworkX export logic from `exporter.py` — dead code divergence risk** — `network.py` implements `build_graph`, `_flatten_attributes`, and all NetworkX write calls. `exporter.py` also implements `_build_nx_graph`, `_nx_flatten_attributes`, and `export_networkx`. The pipeline uses `exporter.export_networkx` exclusively; `network.py`'s `main()` is unreachable from the pipeline. Two implementations of `attr_<name>_value` / `attr_<name>_confidence` attribute key pattern and `_source_files_str` that can silently diverge. Fix: delete `network.py` or make it delegate to `exporter.py`. | 2026-05-17 | 2026-05-17 |
| 108 | [done] | System | **`schema_gap_proposals.json` never invalidated — stale proposals used as correction context on second restart** — `step_orphan_connect.py` writes `schema_gap_proposals.json` directly to disk; it is not listed in any `Step.outputs` and not in `_SCHEMA_RESTART_INVALIDATE`. On a second schema-gap restart, `_fix_schema_extend` reads the stale proposals from the first restart as context for the second restart's LLM correction prompt, producing contradictory guidance. Fix: add `"schema_gap_proposals.json"` to `orphan_connect`'s `Step.outputs` in `pipeline.py`, or explicitly delete it in the restart cleanup block in `orchestrator.py`. | 2026-05-17 | 2026-05-17 |
| 107 | [done] | System | **`step_orphan_connect` schema-extend validation path does not write `schema_validation_errors.json`** — D17 contract: "All validation outcomes (both attempts) are written to `schema_validation_errors.json` for the reviewer." The inline validation in `step_orphan_connect` (lines 174–202) logs results but never writes the file. After a schema-gap Re-entry A, the reviewer has no persistent error record if LLM correction also fails. `step_schema.py`'s path writes this correctly; the orphan-connect path does not. Fix: write `schema_validation_errors.json` after the second validation attempt in `step_orphan_connect`. | 2026-05-17 | 2026-05-17 |
| 106 | [done] | Software | **`timeout` not configurable for Anthropic and OpenAI adapters — Invariant 7 partial violation** — D3 specifies all provider parameters including `timeout` are set via config. `OllamaAdapter` accepts `timeout` from config; neither `AnthropicAdapter` nor `OpenAIAdapter` does. Config factory only passes `timeout` for the Ollama branch. Fix: add `timeout: int | None` to `AnthropicAdapter` and `OpenAIAdapter` constructors; read `section.get("timeout")` in `llm/config.py` for both providers; pass to SDK calls (`messages.create(timeout=...)` and `chat.completions.create(timeout=...)`). | 2026-05-17 | 2026-05-17 |
| 105 | [done] | Software | **`chunk_texts` computed twice in `orphan_connect` — double tiktoken re-chunking cost** — `run_orphan_connect` calls `build_chunk_texts(file_manifest)` at line 54, then `confirm_orphan_edges` calls `build_chunk_texts(file_manifest)` again internally. For large corpora this re-chunks all files twice per orphan-connect run. Fix: add `chunk_texts: dict | None = None` parameter to `confirm_orphan_edges`; pass the already-computed `chunk_texts` from `run_orphan_connect`. | 2026-05-17 | 2026-05-17 |
| 104 | [done] | System | **`session_root/input/` archival copy never read by the pipeline — reproducible re-runs broken** — D32 specifies sessions contain an `input/` copy for reproducibility. `cli.py` sets `ctx.input_dir = input_dir` (the original CLI argument), not `session_root / "input"`. If the original input directory is deleted, `--session <name>` resume fails even though the archived copy exists. Also `_copy_input_files` does not remove stale files, so the archive accumulates deleted files. Fix: after `_copy_input_files`, reassign `ctx.input_dir = session_root / "input"` so the archive is the authoritative input source on resume. | 2026-05-17 | 2026-05-17 |
| 103 | [done] | System | **`run_human_review` contains unreachable dead code that silently no-ops** — `step_schema.py` last line `log.info("Step 4 — schema approved")` is reached only when `ctx.review=True` and the flag doesn't exist — a path the orchestrator prevents by halting before calling the function. Returns `None` without writing the flag, no error raised. Fix: remove the unreachable log line and replace with `raise RuntimeError("invariant violated: run_human_review reached with review=True and no flag")`. | 2026-05-17 | 2026-05-17 |
| 102 | [done] | Software | **`_annotate_aliases` always writes `aliases: []` even when empty — D29 contract violated at intermediate level** — `_annotate_aliases` unconditionally executes `node["aliases"] = sorted(...)` even when the alias list is empty. D29 says field is absent (not `[]`) when normalization is disabled. `deduplicate_nodes` correctly removes empty sets downstream, so final output is correct — but the intermediate state violates D29 and the cleanup relies on downstream logic. Fix: change to `if aliases := sorted(...): node["aliases"] = aliases`. | 2026-05-17 | 2026-05-17 |
| 101 | [done] | Data | **`_ttl_local()` permits dots in Turtle local names — latent parse failure risk** — `_ttl_local()` allows set `[a-zA-Z0-9_\-.]`. If an LLM produces a concept name containing a dot (e.g., `org.Unit`), the resulting local name `org.Unit` can be misparse by rdflib as prefix `org` + separator `.` + local `Unit`. Fix: remove `.` from the allowed character set in `_ttl_local()`, replacing with `_`. | 2026-05-17 | 2026-05-17 |
| 100 | [done] | Data | **D26 Re-entry A documents only the manual path — automated `SchemaUpdatedError` path undocumented** — D26 Re-entry A says "edit `schema.json` → regenerate `schema.ttl` → rerun from Step 3b." The automated path: `orphan_connect` detects schema-gap orphans → `SchemaUpdatedError` → orchestrator invalidates `schema_flatten` + `pass2` onward → restarts from `pass2` (not Step 3b) → skips human review gate. Fix: add a sub-row or annotation to D26 Re-entry A explaining the automated path, its trigger, invalidation set, entry point, and restart cap. | 2026-05-17 | 2026-05-17 |
| 99 | [done] | Data | **`orphan_log.json` field asymmetry between confirmed and rejected events undocumented in D16** — D16 implies both `orphan_edge_added` and `orphan_edge_rejected` events carry the same fields (`ids`, `confidence`, `rationale`, `heuristic_score`, `llm_confidence`). In `step_orphan_connect.py`, rejected events only carry `{event, orphan_id, candidate_id, reason}`. Update D16 to document the two structurally different record shapes explicitly. | 2026-05-17 | 2026-05-17 |
| 98 | [done] | Software | **`pass2` feedback-correction modifies `schema.json` without invalidating `flattened_schema.json`** — When `feedback.apply("schema_validate", ...)` corrects `schema.json` during `schema_validate` step, `flattened_schema.json` is not deleted. `schema_flatten` then checks `_is_done` — `flattened_schema.json` exists — and skips. Pass 2 runs with flattened attributes from the pre-correction schema while prompting against the corrected schema. Fix: add `_cfg.FLATTENED_SCHEMA_FILE` deletion to `_fix_schema` handler in `feedback.py`. | 2026-05-17 | 2026-05-17 |
| 97 | [done] | System | **Add `"schema_flatten"` to `_SCHEMA_RESTART_INVALIDATE`** — when `SchemaUpdatedError` fires, `flattened_schema.json` was stale. Fixed: `schema_flatten` is now in the invalidation set. | 2026-05-17 | 2026-05-17 |
| 96 | [done] | Data | **Add `D31 — Two-Tier Correction Model` to CLAUDE.md** — the automated Re-entry A triggered by `SchemaUpdatedError` from `orphan_connect` is not documented as a named design decision. Document: trigger condition, `propose_schema_additions()` call flow, `SchemaUpdatedError` exception protocol, `schema_max_restarts` config semantics, exact file invalidation set, and what happens when restart limit is exhausted. Add a new sub-row to the D26 Re-entry A table for the automated case. | 2026-05-17 | 2026-05-17 |
| 95 | [done] | Data | **Persist schema gap proposals to `intermediate/schema_gap_proposals.json`** — implemented per to-do #95; `step_orphan_connect.py` writes this file unconditionally when schema-gap orphans exist. Verify D16 row is present. | 2026-05-17 | 2026-05-17 |
| 94 | [done] | Data | **Expand `orphan_log.json` to include rejected candidates** — `orphan_edge_rejected` events now written for Stage 2 rejections. | 2026-05-17 | 2026-05-17 |
| 93 | [done] | System | **Add `orphan_score` and `orphan_connect` to `orchestrator._log_advisory`** — both entries now present. | 2026-05-17 | 2026-05-17 |
| 92 | [done] | Software | **Validate `propose_schema_additions()` against `concepts[]` before writing** — invalid domain/range proposals are now filtered and logged. | 2026-05-17 | 2026-05-17 |
| 91 | [high] | Software | **Add `pass2` to `feedback._HANDLERS`** — currently `feedback.apply("pass2", ...)` is a no-op but `pipeline_state.json` records `llm_correction=True`. At minimum: return bool from `feedback.apply` and only set `llm_correction=True` when a handler ran. Optionally implement `_fix_pass2_extraction` that injects the validation error context on the third attempt. | 2026-05-17 | — |
| 90 | [done] | Data | **Update D16/D30 `orphan_candidates.json` row and remove backward-compat branch** — D16 and D30 now correctly document the dict format `{"candidates": [...], "schema_gap_orphans": [...]}`. | 2026-05-17 | 2026-05-17 |
| 89 | [done] | Data | **Add `schema_approved.flag` and `flattened_schema.json` to D16 table** — both now present in D16. | 2026-05-17 | 2026-05-17 |
| 88 | [done] | Software | **Split `"schema_validate"` feedback handler into `"schema_extend"`** — `_fix_schema_extend` implemented as separate handler, reads `schema_gap_proposals.json` for context, registered in `_HANDLERS`. | 2026-05-17 | 2026-05-17 |
| 87 | [done] | Software | **Make `_build_chunk_texts` public in `orphan_connector.py`** — renamed to `build_chunk_texts`, documented in public API. | 2026-05-17 | 2026-05-17 |
| 86 | [done] | Software | **Add schema attribute backfill for orphan-inferred edges** — `_confirm_one` now backfills schema edge attributes as `{value: null, confidence: 0.0}`. Invariant 6 enforced. | 2026-05-17 | 2026-05-17 |
| 85 | [done] | Software | **Convert `SchemaUpdatedError` restart from recursion to iteration** — `orchestrator.run()` now uses `while True` + restart flag instead of recursive call. Stack growth eliminated. | 2026-05-17 | 2026-05-17 |
| 84 | [done] | Software | **Remove dead code `feedback._fix_ttl`** — deleted from `feedback.py`. | 2026-05-17 | 2026-05-17 |
| 83 | [low] | Software | **Move `feedback` import to module scope in `orchestrator.py`** — `feedback` is imported inside `run()` with a `_feedback_available` fallback flag never exercised. Move to standard module-level import. | 2026-05-17 | — |
| 82 | [done] | System | **Define library public surface in `__init__.py`** — `__init__.py` exports only `__version__`. D18 says library is primary. Add re-exports of `PipelineContext`, `run`, `STEPS`, `load_adapter` and an `__all__` list so programmatic embedders can discover the API without reading source. | 2026-05-17 | 2026-05-17 |
| 81 | [low] | Software | **Validate proposed schema additions against `concepts[]` before `_SCHEMA_RESTART_INVALIDATE` fires** — redundant with #92 (done) but the earlier guard in `orphan_connector.propose_schema_additions` is cleaner than the step-level check alone. | 2026-05-17 | — |
| 80 | [medium] | Data | **Log unconnectable orphans (zero schema-compatible property pairs) as advisory warnings** — after Stage 1, any orphan with no valid schema property should emit `log.warning(...)` and write `"event": "orphan_unconnectable"` to `orphan_log.json`. | 2026-05-17 | — |
| 79 | [medium] | Software | **When orphan type has no compatible property pairs, skip type filter and pass all co-occurring nodes to Stage 2** — adds LLM cost but recovers currently silent orphans. Stage 2 prompt lists only valid schema properties so it returns `connected: false` when none apply. | 2026-05-17 | — |
| 78 | [done] | Data | **Extend `score_orphan_candidates` to treat subclass relationships as compatible** — walk the inheritance chain upward when building `valid_pairs`. Zero LLM cost; handles cases like `Army Air Forces` (typed `Organization`) where a subtype property exists but the parent type has no direct property. | 2026-05-17 | 2026-05-17 |
| 77 | [critical] | Data | **Root cause: `Army Air Forces` typed as `Organization` but should likely be `MilitaryUnit` or `MilitaryOffice`** — check `schema.json`; if hierarchy makes it a valid peer, fix is `--from-step pass2`; if schema is missing a property, fix is Re-entry A. | 2026-05-17 | — |
| 76 | [done] | System | **Add `orphan_score` and `orphan_connect` to `orchestrator._log_advisory`** — done (duplicate of #93). | 2026-05-17 | 2026-05-17 |
| 75 | [done] | Software | **Register `orphan_connect` feedback handler in `feedback._HANDLERS`** — handler reads `orphan_candidates.json` + `orphan_connections.json`, builds correction prompt for invalid edges, calls LLM for re-evaluation. | 2026-05-17 | 2026-05-17 |
| 74 | [done] | Software | **Add `count_tokens(text: str) → int` to `chunker.py`** — implemented. | 2026-05-17 | 2026-05-17 |
| 73 | [done] | Software | **Implement `confirm_orphan_edges()` Stage 2 LLM confirmation** — implemented with source text excerpts, all-mentions strategy, confidence formula. | 2026-05-17 | 2026-05-17 |
| 72 | [done] | Software | **Implement `build_orphan_candidates()` Stage 1 co-occurrence algorithm** — implemented with chunk inversion, schema filter, heuristic scoring, top-k cap. | 2026-05-17 | 2026-05-17 |
| 71 | [done] | Software | **Create `orphan_connector.py` with full public API** — implemented. | 2026-05-17 | 2026-05-17 |
| 70 | [done] | System | **Register `orphan_score` and `orphan_connect` steps in `pipeline.py`** — both steps registered between `assemble` and `export`. | 2026-05-17 | 2026-05-17 |
| 69 | [done] | Data | **Write `intermediate/orphan_candidates.json` from Stage 1** — implemented. | 2026-05-17 | 2026-05-17 |
| 68 | [done] | Data | **Write `intermediate/orphan_connections.json` from Stage 2** — implemented. | 2026-05-17 | 2026-05-17 |
| 67 | [done] | Data | **`step_orphan_connect`: merge `orphan_connections.json` into `edge_metadata.json`** — merged output contract adopted. | 2026-05-17 | 2026-05-17 |
| 66 | [done] | Data | **Add `"method": "llm_extraction"` to all edges in `assembler.py:deduplicate_edges`** — implemented; D8 updated. | 2026-05-17 | 2026-05-17 |
| 65 | [done] | Data | **Write `intermediate/orphan_log.json`** — implemented with accepted and rejected events. | 2026-05-17 | 2026-05-17 |
| 64 | [done] | Data | **Add `orphan_pass` config section to `pipeline_config.yaml`** — implemented; constants exposed via `config.py`. | 2026-05-17 | 2026-05-17 |
| 63 | [done] | Software | **Modify `pass2._process_file` to build `chunk_node_index.json`** — implemented; nested dict format `{filename: {chunk_idx: [ids]}}`. | 2026-05-17 | 2026-05-17 |
| 62 | [done] | Software | **Add 10 unit tests for `orphan_connector.py`** — 13 tests implemented. | 2026-05-17 | 2026-05-17 |
| 61 | [done] | System | **Add `ctx.chunk_node_index` to `PipelineContext`** — implemented. | 2026-05-17 | 2026-05-17 |
| 60 | [done] | Data | **Update D16 in CLAUDE.md with all Option E intermediate files** — updated. | 2026-05-17 | 2026-05-17 |
| 59 | [done] | System | **Add Re-entry D to CLAUDE.md D26** — added. | 2026-05-17 | 2026-05-17 |
| 43 | [done] | Data | **Lossless merge for confidence-1.0 string ties** — concatenate with `"; "` instead of first-wins. | 2026-05-16 | 2026-05-16 |
| 1 | [done] | Software/Data | **Pydantic migration** — all 6 dataclasses replaced with Pydantic `BaseModel`. | 2026-05-16 | 2026-05-16 |
| 2 | [done] | Data | **Deduplicate stable ID generation** — shared `ids.py` module. | 2026-05-16 | 2026-05-16 |
| 3 | [done] | System | **Fix `--llm-config` flag** — removed; config must be on search path. | 2026-05-16 | 2026-05-16 |
| 4 | [done] | Software | **Fix `OllamaAdapter` ignoring `max_tokens`** — fixed. | 2026-05-16 | 2026-05-16 |
| 5 | [done] | Software | **Fix `validate_extraction` top-level key check** — explicit structural check added. | 2026-05-16 | 2026-05-16 |
| 6 | [done] | System | **Prevent `schema_validate` from silently overwriting approved schema** — sentinel file added. | 2026-05-16 | 2026-05-16 |
| 7 | [done] | System/Software | **Fix `merge_log.json` accumulation on re-runs** — dual ownership removed. | 2026-05-16 | 2026-05-16 |
| 8 | [done] | Software | **Fix stale `llm_config.json` references in adapter error messages** — updated to `pipeline_config.yaml`. | 2026-05-16 | 2026-05-16 |

---

## Change Log

| 2026-05-25 | Third four-agent architecture review. 21 new to-do items (#146–#165): 5 critical (stale flattened schema on merge restart, merge_raw re-entry collision, shard `_fname` namespacing in re-extraction, walkthrough ABox key mismatch, cold re-entry RuntimeError from missing session context), 4 critical adversarial (health status always clean, self-merge audit mislead, schema-gap restart limit leaves step `"running"`, merge orchestrator uses wrong restart constant), 5 high (missing `schema_validate` in MERGE_SCHEMA_RESTART_INVALIDATE, missing `--from-step` for merge CLI, shard filename collision, wrong config constant in step_orphan_connect for merge, serial session re-extraction), 7 medium/low (walkthrough count probe wrong field, merge_manifest cold-re-entry empty deltas, quality-review empty schema acceptance, Invariant 7 partial violations, pipeline_state cosmetic, D16/D36/architecture.md doc gaps). Key confirmed resolutions: #91 (pass2 feedback no-op) and #81 (propose_schema_additions validation) both confirmed resolved in new code. | Third four-agent parallel review after merge pipeline implementation |
| 2026-05-21 | Documentation sync: added D35 (3-stage Pass 1 induction), D36 (schema_history module), D37 (surgical re-extraction on schema-gap restart) to CLAUDE.md; corrected D3 to list all 5 LLM adapters; expanded config documentation in CLAUDE.md, README.md, and implementation-alternatives.md; updated architecture.md current state assessment and design decisions summary; added Option E (chunk-level orphan pass) and 3-stage schema induction sections to implementation-alternatives.md | Post-release documentation sweep to sync docs with implemented codebase |
| 2026-05-18 | Second four-agent architecture review. 20 new to-do items (#124–#143): 3 critical (ErrorGate race condition, Re-entry B shard leak, `confirmed_orphan_ids` direction bug), 5 high (schema-gap restart shard leak, `--from-step` without `--session`, error-rejected orphan promotion, duplicate tests, outdated pipeline-workflow.md), 7 medium/low. Key invariant violations confirmed: Invariant 5 (Relationship class bypass), Invariant 7 (hardcoded timeout), Invariant 8 (plain dict pipeline boundaries). New `docs/pipeline-workflow.md` v2 written reflecting current parallel architecture. | Second four-agent parallel review after ErrorGate + Pass 1 parallelism + per-file sharding implementation |
| 2026-05-18 | Added `ErrorGate` (src/mykg/llm/error_gate.py) — global thread-safe pause-on-error gate; wired into all adapters via `retry_on_rate_limit`, into `PipelineContext`, and into pass1/pass2/orphan_connect `as_completed` loops. Reduces `max_workers` to 2 for ollama-local profile; reduces `batch_token_target` to 170000; adds 'q' abort path at pause prompt. 296 tests pass. | Ollama cloud quota exhaustion cascaded across all 4 workers; needed coordinated pause rather than per-call retry |
| 2026-05-18 | Implemented to-do #120: `step_normalize.py` now builds an `id_remap` dict from `norm_map` after `apply_normalization_map()` runs and walks `chunk_node_index.json` replacing pre-normalization stable IDs with canonical ones; shard files in `chunk_index_shards/` updated in the same pass; `normalize_names` Step outputs extended with `"chunk_node_index.json"` so `_is_done` treats it as stale on re-entry. 3 new tests; 296 tests pass. | Orphan Stage 1 was silently scoring normalized aliases as orphans due to stale IDs |
| 2026-05-18 | Implemented to-dos #116, #117, #121, #122, #123 in 5 parallel agents: HTTP 429 backoff with shared `retry_on_rate_limit` helper in `llm/retry.py` (#123); Pass 1 JSON parse retry on JSONDecodeError (#122); Pass 1 parallel batch dispatch via `ThreadPoolExecutor` with sorted result collection (#117); parallel ingest with `_ingest_file`/`_read_and_hash` pure workers, deterministic sorted merge (#116); per-file shard writes for `raw_extractions_shards/` and `chunk_index_shards/` replacing O(N²) full rewrites (#121). 293 tests pass. | Five high/critical bottlenecks from session 2026-05-18T07-09-04 |
| 2026-05-18 | Added 6 new to-do items (#118–#123) from workflow bottleneck analysis of session 2026-05-18T07-09-04: HTTP 429 backoff (#123 critical), Pass 1 JSON retry (#122 high), O(N²) raw_extractions rewrite (#121 high), chunk_node_index ID drift (#120 medium), triple tokenization (#119 medium), prior_nodes context overflow (#118 medium). Full workflow doc at `docs/pipeline-workflow.md`. | Workflow diagram surfaced 12 bottlenecks; prioritized by severity from session data |
| 2026-05-18 | Added Invariant 13 (LLM context and rate limit respect) to CLAUDE.md and architecture.md | Session 2026-05-18T07-09-04 hit HTTP 429 3 times losing all Pass 2 progress; codified as invariant |
| 2026-05-18 | Added Invariant 12 (mandatory parallelism) to CLAUDE.md and architecture.md; added to-do #116 for parallel ingest with full four-agent design elaboration | Identified serial ingest loop as Invariant 12 violation; planned implementation before coding |
| 2026-05-17 | Implemented all 21 open to-do items (98–115 + #78, #75, #82, #95, #96, #100) in 8 parallel agent batches: 5 critical fixes (step_pass1 manifest crash, orphan confidence clamping, schema_validate invalidation, review gate bypass, ghost nodes in _partial_recover), 9 high fixes (validate_extraction field checks, network.py deletion, schema_gap_proposals.json output tracking, schema_validation_errors.json in orphan_connect path, adapter timeout, chunk_texts dedup, session input dir, orphan_connect feedback handler, inheritance chain walking), and 7 medium/low fixes (flattened_schema deletion, D26 automated Re-entry A docs, orphan_log field asymmetry, _ttl_local dots, _annotate_aliases empty list, run_human_review dead code, __init__.py public API). 254 tests pass. | Full implementation sweep of latest architecture review |
| 2026-05-17 | Four-agent parallel review (System, Software, Data, Adversarial); 18 new to-do items (98–115) covering: `--from-step pass1` crash on new manifest format (critical), unclamped orphan confidence (critical), `schema_validate` not invalidated on schema restart (critical), human review gate bypassed on restart (critical), ghost nodes from `_partial_recover` (critical), `validate_extraction` missing node field checks, `network.py` dead-code duplication, `schema_gap_proposals.json` not invalidated, schema-extend path missing validation error file, Anthropic/OpenAI timeout config, double `build_chunk_texts` call, session input dir not used on resume, D26 automated Re-entry A undocumented, `orphan_log.json` field asymmetry, feedback-correction not invalidating flattened schema, `_ttl_local` dot risk, dead code in `run_human_review`, `_annotate_aliases` empty-list D29 violation | First review after full orphan-pass implementation; checks resolution of all prior to-dos and audits new code paths |
| 2026-05-17 | Named and documented the two-tier correction model (Tier 1 KG-level, Tier 2 schema-level escalation); identified critical bug: `schema_flatten` not in `_SCHEMA_RESTART_INVALIDATE`; 17 new to-do items (81–97) covering correctness fix, schema gap proposal audit file, orphan log expansion, feedback handler split, recursion→iteration conversion, dead code removal, D16 documentation gaps, D31 design decision | Three-agent parallel review of the two-tier correction loop |
| 2026-05-17 | Implemented Option E two-stage orphan pass: `chunk_node_index.json` built by `pass2._process_file`; `OrphanCandidate` Pydantic model; `orphan_connector.py` with Stage 1 co-occurrence heuristic + Stage 2 LLM confirmation with source text context; `step_orphan_score` + `step_orphan_connect` registered in `pipeline.py`; `file_manifest.json` loaded for chunk text extraction; `orphan_connections.json` merged into `edge_metadata.json` before export; `"method"` field on all edges; config `orphan_pass` section; D8/D16/D26/D30 updated in CLAUDE.md; 13 unit tests | Option E selected as best accuracy/cost tradeoff |
| 2026-05-17 | Refined Stage 2 LLM prompt: `_extract_relevant_excerpt()` finds all name mentions in shared chunks, merges overlapping windows, caps at 1600 chars | Confirmed-edge count increased from 2→6; first-match was insufficient for entities appearing multiple times per chunk |
| 2026-05-17 | Three-agent parallel concrete implementation plan for Option E; 21 new to-do items (56–76); two-step registration, `chunk_node_index.json`, all intermediate file schemas, merged output contract, `"method"` field, confidence formula, 10 unit tests, D16/D26 updates | Committing to Option E before code was written |
| 2026-05-16 | Three-agent parallel design review of orphan-connection options (A–E); 12 new to-do items (44–55) | No orphans connected in current run; established architecture before implementation |
| 2026-05-16 | Added NetworkX multi-format export (Step 12c); `networkx` added as dependency | Enables direct graph analysis without a separate conversion step |
| 2026-05-16 | Fixed all consistency issues (items 33–42): `merge_log.json` dual ownership, `all_chunks` recovery, `aliases: []` vs absent, `_find_match`/`synonym_match` duplication, ABox header conditional, name normalizer tests, D19/D26/D16 doc gaps | Consistency audit after Option B + aliases implementation |
| 2026-05-16 | Implemented all 32 open to-do items: Option B normalization pass, node aliases, `skos:altLabel` in TTL, TTL validator external-namespace whitelist, `file_manifest.json` persistence, all docs updated | Completing the full architecture roadmap |
| 2026-05-16 | Fixed `_normalise` in `schema_merge.py` to split PascalCase | `"SoftwareEngineer"` ≠ `"software_engineer"` caused duplicate schema types |
| 2026-05-16 | Implemented all 18 architecture to-do items from first review: Pydantic migration, shared `ids.py`, `merge_log.json` fix, `schema_validate` sentinel, `OllamaAdapter` `max_tokens`, `validate_extraction` structural check, `feedback.apply()` routing, `_coerce_attr` hardening, `base_schema_parsed.json` fix, `SynonymIndex` public API, docs cleanup | First architecture review identified 20 issues; 18 implemented |

---

## Subagent Findings (latest review — 2026-05-25, third review)

### System Architect

**Confirmed working correctly:**
- Merge pipeline correctly mirrors the extract pipeline's orchestration contract (`PipelineState` / `_is_done` / retry / non-blocking pattern)
- `merge_run.py` `SchemaUpdatedError` handler uses `while True` + iterative break — no recursion (to-do #85 applied to both orchestrators)
- File namespacing in `merge_setup` is structurally sound — `_copy_shard_dir` rewrites `_fname` and renames disk files before copying
- Merge schema chain is complete: `merge_proposals()` + `harmonize_schema()` + `review_schema_quality()` — matches D35/D38
- `merge_raw.py` correctly builds `chunk_node_index.json` from namespaced shards for orphan pass
- Surgical re-extraction correctly targets only affected chunks; fallback to full enumeration when index is unavailable
- `#91` (pass2 feedback no-op) confirmed resolved — `feedback._HANDLERS` has no `"pass2"` key; `apply()` correctly returns False
- `#81` (validate schema additions against concepts[]) confirmed resolved — `propose_schema_additions()` filters invalid domain/range

**New issues found:**
1. `_MERGE_SCHEMA_RESTART_INVALIDATE` missing `"schema_flatten"` — stale flat schema used on merge schema-gap restart **[critical, #165]**
2. `merge_raw` re-entry breaks when sessions share filenames — `ValueError` on key collision **[critical, #164]**
3. `merge_reextract` shard files written with un-namespaced `_fname` — orphan pass fails on re-entry **[critical, #163]**
4. `_MERGE_SCHEMA_RESTART_INVALIDATE` missing `"schema_validate"` — RDFS validation bypassed on restart **[high, #159]**
5. `merge-graphs` CLI has no `--from-step` flag — documented re-entry hints reference nonexistent flag **[high, #158]**
6. `step_orphan_connect` uses `ORPHAN_SCHEMA_MAX_RESTARTS` (extract config) in merge pipeline — config mismatch **[high, #156]**
7. `merge_reextract` calls sessions A and B serially — Invariant 12 violation **[medium, #155]**
8. `pipeline_state.json` step left in `"running"` state when restart limit hit — both orchestrators **[low, #149]**

---

### Software Architect

**Confirmed working correctly:**
- `MergeContext(PipelineContext)` via Pydantic subclassing — Liskov-safe polymorphism, all extract steps accept MergeContext unchanged
- All new merge modules use Pydantic models — `SessionData(BaseModel)`, `MergeContext(PipelineContext)` — Invariant 8 holds in new code
- Invariant 7 holds in new code — all merge constants use `_get()`/`_get_opt()` without hardcoded literals
- D3 LLM adapter interface is clean — adding a new provider requires only a new file implementing `complete()` and `endpoint_label()`
- ANSI color logging correctly scoped — `_ColorFormatter` applied only to stdout handler when `isatty()` is True; never in log files
- `#91` (pass2 feedback handler no-op): confirmed resolved — `feedback._HANDLERS` returns False for unknown step, `llm_correction` not set
- `#81` (validate schema additions): confirmed resolved — `propose_schema_additions()` validates domain/range against `declared_types`
- `_backfill_extraction` null-fills all attributes from flat schema — Invariant 6 enforced for LLM-extracted edges

**New issues found:**
1. Dead import of `_log_advisory` from `orchestrator` in `merge_run.py` — never called; local `_log_merge_advisory` is used instead **[low, cleanup]**
2. `session_a: Any | None = None` in MergeContext — `Any` type avoids circular import but loses type safety for `SessionData` fields **[low, cosmetic]**
3. `merger.py` handles session loading, namespacing, schema delta, re-extraction dispatch, and raw merging — cohesion concern, not a bug **[low, observation]**
4. `ctx.session_a.raw_extractions` mutated in-place by `step_merge_reextract` — if step is re-entered in same process, partial-namespaced state is fed back **[medium, related to #164]**
5. **`#83` (feedback import at module scope) still open** — deferred import pattern works but non-idiomatic; no change since last review

---

### Data Architect

**Confirmed working correctly:**
- `source_map.json` format is complete — all D38-required fields present: `session`, `session_alias`, `original_path`, `sha256`, `role`; `_meta` block has both session names and `prep_mode`
- `merge_manifest.json` fields match D38 exactly: `session_a`, `session_b`, `merged_at`, `schema_synonym_log`, `reextraction_strategy`, `schema_delta_session_a`, `schema_delta_session_b`
- File namespacing applied consistently to shard filenames and raw extraction keys — `_fname`, dict keys, and `source_files` lists all namespaced
- Node deduplication produces same stable ID regardless of source session — `ids.py` `stable_id()` is deterministic; cross-session same-type+name correctly merges
- Edge deduplication key correct per D22 — `hash(type + from_id + to_id)` across fully namespaced merged extractions
- `knowledge_graph.ttl` is RDFS-pure — no edge metadata, no blank nodes, no reification — D14 satisfied
- Orphan pass operates correctly on namespaced keys in merge sessions — `chunk_node_index.json` built from pre-namespaced shards, keys match

**New issues found:**
1. `architecture.md` surgical re-extraction description contradicts D38 and Invariant 15 — says "no new nodes" but implementation allows them **[medium, #154]**
2. Walkthrough merge count probe uses wrong field (`type is None` vs `event == "node_merge"`) — all merge events appear as node merges **[medium, #153]**
3. D16 missing rows for `source_map.json`, `merge_manifest.json`, `merge_reextract.done` **[low, #148]**
4. D36 trigger label list missing `"session_merge"` — written by `step_merge_schema` but not in D36 **[low, #147]**
5. `architecture.md` MERGE_STEPS table says 10 steps; implementation has 12 **[low, #146]**
6. `merge_schema` writes only one `schema_history` entry for a three-stage chain — extract pipeline writes three separate delta entries (pass1_merge, schema_harmonize, schema_quality); merge pipeline writes one **[low, observation]**
7. `normalize_names` absent from merge pipeline — cross-session name variants not normalized before deduplication; documented in D38 open question **[open, Q1]**

---

### Adversarial Architect

**Critical Failure Paths (new — merge pipeline attack surface):**

1. **Self-merge: session A == session B produces misleading audit trail, exit code 0** — No guard prevents `mykg merge-graphs X X`. Final graph is structurally correct (deduplication collapses), but `source_map.json`, `merge_manifest.json`, and walkthrough all report session_a=X and session_b=X as a valid cross-corpus merge. **[critical, #160]**

2. **Non-atomic `merge_setup`: crash after `source_map.json` write but before `file_manifest.json` write — orphan pass receives empty chunk context** — `_is_done("merge_setup")` sees `source_map.json` and returns True. `file_manifest.json` is absent. Orphan Stage 2 LLM calls receive empty chunk text. Zero connections. No error raised. **[critical, related to #161]**

3. **`merge_raw` cold re-entry after `merge_reextract` done: `ctx.session_a is None` → RuntimeError** — Fresh process starts; `merge_setup` skipped (done); `ctx.session_a` is None; `run_merge_raw` raises RuntimeError. Re-entry impossible without deleting `merge_reextract.done`. Re-entry hint doesn't mention this. **[critical, #161]**

4. **`merge_reextract` writes shards with un-namespaced `_fname` — double-namespacing on re-entry produces `session_a/session_a/notes.md` keys** — In-memory namespacing is correct; on-disk shards use un-namespaced `_fname`. On re-entry, `merge_raw` reads both namespaced originals and un-namespaced re-extraction shards; orphan pass gets double-namespaced keys that match nothing. All orphans become schema-gap orphans. **[critical, #163]**

5. **Walkthrough health status always reads "Clean" for ABox — wrong key `"abox_errors"` vs correct `"abox_checks".errors`** — Health banner says "Clean" even when real ABox errors exist. Final Graph Summary section is correct. Direct contradiction within same walkthrough report. **[critical, #162]**

6. **Shard filename collision — two files with same slug produce same destination path, last-writer-wins** — `_copy_shard_dir` sha1 truncation (16 chars) creates collision risk for long filenames. Silent overwrite; affected file's extractions are permanently absent. **[high, #157]**

7. **`step_orphan_connect` uses `ORPHAN_SCHEMA_MAX_RESTARTS` (extract constant) in merge pipeline** — When the two constants diverge, schema.json is updated inside `step_orphan_connect` before the merge orchestrator can block the restart. Spurious properties permanently enter the merged schema. **[high, #156]**

8. **`review_schema_quality_for_merge` can accept `{"concepts": [], "properties": []}` — complete graph loss** — Empty TBox is valid RDFS; validation passes; `merge_reextract` rejects every extraction (unknown type); all outputs are empty but structurally valid. **[medium, #151]**

**Invariant Violation Analysis:**
- **Invariant 6** — Violated by #165: when `_MERGE_SCHEMA_RESTART_INVALIDATE` omits `"schema_flatten"`, re-extracted edges for new properties have `attributes: {}` — null-fill only applies to attributes present in the (stale) flat schema
- **Invariant 7** — `config.py` uses `_eg.get("enabled", True)` / `_eg.get("threshold", 3)` with Python-level defaults; `_get_opt` for merge constants — absent YAML sections silently apply defaults. (tracked, #150)
- **Invariant 8** — `ctx.session_a.raw_extractions` is `dict` in `SessionData`; raw extraction dicts passed between merge stages are not Pydantic models (plain dicts loaded from JSON). (open, tracked)
- **Invariant 9** — Partially violated: self-merge and concurrent-run scenarios create cross-run dependencies that can corrupt audit trails (self-merge) or intermediate state (concurrent runs)
